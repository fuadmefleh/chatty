server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback - react-router-dom routes are client-side, so any path
    # that doesn't match a real static asset should still get index.html.
    location / {
        try_files $uri /index.html;
    }
}
